from django.apps import AppConfig                     # 0.13 App config system


class BlogConfig(AppConfig):                          # 0.14 Define app config
    name = 'blog'                                     # 0.15 App name (must match folder name)